
import { Topic, Language, VoiceStyle } from './types';

export const TOPIC_OPTIONS: string[] = Object.values(Topic);
export const LANGUAGE_OPTIONS: string[] = Object.values(Language);
export const VOICE_STYLE_OPTIONS: string[] = Object.values(VoiceStyle);
