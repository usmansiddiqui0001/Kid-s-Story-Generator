import React from 'react';

// This component is obsolete. API keys must be configured via the process.env.API_KEY environment variable.
const ApiKeysModal: React.FC = () => null;

export default ApiKeysModal;
